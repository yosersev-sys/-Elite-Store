
DirectoryIndex index.php

<IfModule mod_rewrite.c>
    RewriteEngine On
    # توجيه جميع الطلبات إلى index.php إذا لم يكن الملف موجوداً فعلياً
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>
